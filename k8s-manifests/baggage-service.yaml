apiVersion: apps/v1
kind: Deployment
metadata:
  name: baggage-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: baggage-service
  template:
    metadata:
      labels:
        app: baggage-service
    spec:
      containers:
        - name: baggage-service
          image: YOUR_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/aerolink/baggage-service:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "production"
            - name: AWS_REGION
              value: "us-east-1"
---
apiVersion: v1
kind: Service
metadata:
  name: baggage-service
  namespace: default
spec:
  selector:
    app: baggage-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
